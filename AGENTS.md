## Credential Governance

Windows 凭证管理器中的 `github key token` 属于高级权限专属凭证。它只能用于仓库发布、引用更新等需要提升权限的操作；使用前必须在当前对话中获得用户的明确批准。

未获批准时，Git 和 `gh` 应使用各自凭证链中的普通凭证，不得读取、导入、切换或复制该凭证。任何情况下都不得把 token 明文写入仓库、日志、脚本、环境变量、README、备份或全局 Git 配置。
